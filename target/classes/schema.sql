CREATE TABLE `category_user`(
	`id` INT(11) NOT NULL,
	`event1` VARCHAR(50) NULL DEFAULT NULL,
	`event2` VARCHAR(50) NULL DEFAULT NULL,
	`event3` VARCHAR(50) NULL DEFAULT NULL,
	`event4` VARCHAR(50) NULL DEFAULT NULL,
	`event5` VARCHAR(50) NULL DEFAULT NULL,
	PRIMARY KEY (`id`)
);
